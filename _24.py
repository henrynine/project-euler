'''
make all permutations
sort
get millionth
'''
import common, functools, operator

digits = [str(n) for n in range(10)]

permutations = []
for b in common.get_binaries(10)

def permutations(n):
    digits = [str(n) for n in range(n)]
    


print(original_str)
